// Optimized Bootstrap initialization
document.addEventListener('DOMContentLoaded', () => {
    // Language dropdown functionality is now handled by Bootstrap's native dropdown
    // This file is kept for future custom initializations if needed
});